# Fabric Scaffold Scripts

These scripts are placeholders for the MVP Fabric network lifecycle.

- `bootstrap-network.sh` documents the planned bootstrap sequence.
- `generate-connection-profiles.sh` documents client profile generation.

They are intentionally non-destructive and do not try to launch Fabric containers.
